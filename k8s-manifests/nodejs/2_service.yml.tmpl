apiVersion: v1
kind: Service
metadata:
  name: {{ .EnvPrefix }}-{{ .SlugName }}-svc
  namespace: {{ .Namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Name }}
  ports:
  - name: http
    port: 80
    targetPort: app-port
