{{- if .Values.create.tektonResources -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "tekton.app-secret.name" . }}
  namespace: {{ .Values.tekton.namespace }}
type: Opaque
stringData:
  env: {{ .Values.environment }}
  env-prefix: {{ .Values.envPrefix }}
  app-name: {{ .Values.appName }}
  app-slug-name: {{ .Values.appSlugName }}
  product-name: {{ .Values.productName }}
  github-token: {{ .Values.secretData.githubToken }}
  ssh-deploy-key: {{ .Values.secretData.sshDeployKey }}
  ecr-repo-url: {{ .Values.secretData.ecrRepoUrl }}
{{- end -}}
